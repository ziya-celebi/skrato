name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: arch based
    steps:
      - uses: actions/checkout@v4
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Build
        run: go build -v ./...