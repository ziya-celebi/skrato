name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: arch based
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Build (release)
        run: cargo build --release --verbose


